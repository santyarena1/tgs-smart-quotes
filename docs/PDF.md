# PDF

Se renderiza HTML/CSS A4 con Chromium/Playwright y se guarda el binario exacto. SIMPLE agrupa una PC y oculta precios individuales; DETALLADO expone cantidad, unitario y subtotal. La configuracion resuelta se congela por version. Nunca se muestra validez ni vencimiento.
