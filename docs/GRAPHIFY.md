# Graphify

Instalado con `uv tool install graphifyy`, `graphify install --project --strict` y hooks Git. `.mcp.json` apunta a `graphify-out/graph.json`. Actualizar con `graphify update .`; cerrar con `graphify extract . --mode deep` (requiere una clave LLM) o `--code-only` cuando no existe clave.
