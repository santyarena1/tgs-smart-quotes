import{describe,expect,it}from'vitest';import{applyCost,applyMarkup,applySale,markupFromPrices,retarget,saleFromCost,totals}from'./index.js';
const item={id:'a',costCents:10000n,salePriceCents:13000n,markupBps:3000,quantity:1,position:0};
describe('pricing entero',()=>{it('calcula costo, venta y markup con half-up',()=>{expect(saleFromCost(101n,500)).toBe(106n);expect(markupFromPrices(10000n,13000n)).toBe(3000)});it('aplica reglas bidireccionales',()=>{expect(applyCost(item,20000n).salePriceCents).toBe(26000n);expect(applyMarkup(item,5000).salePriceCents).toBe(15000n);expect(applySale(item,12500n).markupBps).toBe(2500)});it('totaliza cantidades',()=>{expect(totals([{...item,quantity:2}])).toEqual({costCents:20000n,saleCents:26000n,profitCents:6000n,effectiveMarkupBps:3000})});it('retargetea y asigna residuo estable al mayor subtotal',()=>{const b={...item,id:'b',costCents:5000n,salePriceCents:6500n,position:1};const r=retarget([item,b],18001n);expect(r.preview.saleCents).toBe(18001n);expect(r.items[0]?.salePriceCents).toBe(12000n)});it('acepta un objetivo por debajo del costo (ganancia negativa)',()=>{const r=retarget([item],9000n);expect(r.preview.saleCents).toBe(9000n);expect(r.items[0]?.markupBps).toBe(-1000)});it('con ganancia actual cero reparte el mismo markup a todos',()=>{const r=retarget([{...item,salePriceCents:10000n,markupBps:0},{...item,id:'b',costCents:5000n,salePriceCents:5000n,markupBps:0,position:1}],18000n);expect(r.preview.saleCents).toBe(18000n);expect(r.items[0]?.markupBps).toBe(2000);expect(r.items[1]?.markupBps).toBe(2000)});it('el markup nunca baja de -100 % ni hay venta negativa',()=>{expect(()=>saleFromCost(100n,-10001)).toThrow();expect(markupFromPrices(10000n,5000n)).toBe(-5000)})});

